import React, { useState } from 'react';
import Modal from '../Modal/Modal';

const Homepage = () => {
    return (
        <div>
            <Modal />
        </div>
    );
};

export default Homepage;
